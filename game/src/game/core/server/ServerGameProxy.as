package game.core.server
{
	import game.core.IGameProxy;
	
	import org.puremvc.as3.patterns.proxy.Proxy;
	
	public class ServerGameProxy extends Proxy implements IGameProxy
	{
		public function ServerGameProxy()
		{
			
		}
		
		public function connect(connectionData:Object = null):void
		{
			
		}
	}
}